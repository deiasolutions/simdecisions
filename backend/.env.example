# Database connection
# SQLite (local dev):
DATABASE_URL=sqlite:///./hive.db

# PostgreSQL (Railway):
# DATABASE_URL=postgresql://user:pass@host:5432/dbname

# CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:3000

# API Admin Key (for authentication - change in production!)
API_ADMIN_KEY=dev-admin-key-change-in-prod

# Log level
LOG_LEVEL=INFO
